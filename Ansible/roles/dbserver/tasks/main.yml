# tasks file for roles/dbserver
---
- name: Installera MariaDB-server
  ansible.builtin.package:
    name: mariadb-server
    state: present

- name: Starta och aktivera MariaDB-tjänsten
  ansible.builtin.service:
    name: mariadb
    state: started
    enabled: true
